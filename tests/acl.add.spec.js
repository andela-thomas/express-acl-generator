// (function() {
    //   'use strict';

    //   var program = require('../lib/program');
    //   var assert = require('assert');
    //   var fs = require('fs');
    //   var config;
    //   var args, group, path;

    //   describe('start test', function() {
    //     describe('should', function() {
    //       beforeEach(function(done) {
    //         args = {};
    //         args.options = {};
    //         args.group = 'guest';
    //         path = 'config.json';
    //         console.log(program.add.group(fs, args));
    //         done();
    //       });


    //       it('should return ', function(done) {
    //         var args = {};
    //         args.group = 'guest';
    //         config = require('../config.json');
    //         config = JSON.parse(config);

    //         group = config.find(function(el) {
    //           return el.group === args.group;
    //         });

    //         var permissions = group.permissions;
    //         assert(group, true);
    //         assert(typeof group, 'object');
    //         assert(permissions, true);
    //         assert(Array.isArray(permissions), true);
    //         assert(permissions[0].resource, true);
    //         assert(permissions[0].methods, true);
    //         assert(group.action, true);
    //         assert.deepEqual(group.action, 'allow');


    //         done();
    //       });
    //     });

    //   });

    // })();
